# Python
__pycache__/
*.pyc
dist/
build/
.coverage
.pytest_cache/
.ruff_cache/
.venv/

# MC Translator runtime artifacts
logs/
*.bak-*
cache.json
ai_cache.json

# Local settings -- contains the user's absolute mc_dir path and, once
# filled in, plaintext DeepL/OpenRouter API keys. See settings.ini.example
# for the template that IS committed.
settings.ini

# Project archives -- not build output, just large ad-hoc zips of the repo
*.rar

# Ruflo / claude-flow runtime artifacts
.swarm/
.claude-flow/
.claude/memory.db
ruvector.db
data/

# Local Claude Code / claude-flow tooling scaffolding (auto-generated by
# ruflo init) -- machine-level tooling config, not MC Translator source.
.claude/
.mcp.json
skills-lock.json

# Personal Obsidian vault / project journal -- lives outside the repo now.
Progect-Minecraf-Translate/
