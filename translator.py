"""Entry point for MC Translator (python translator.py).

With no arguments, launches the GUI. With arguments, runs headless
(see `python translator.py --help`).
"""
from mc_translator.__main__ import main

if __name__ == "__main__":
    main()
